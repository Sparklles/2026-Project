<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.productmanagement.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.productmanagement.entity.User">
        <id property="id" column="id"/>
        <result property="loginAccount" column="login_account"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, login_account, password, phone, email, role,
        status, last_login_time, is_deleted, create_time, update_time
    </sql>

    <!-- 后台用户列表：联表查询 resultMap -->
    <resultMap id="AdminUserListVoMap" type="com.example.productmanagement.vo.AdminUserListVo">
        <result property="userId"        column="id"/>
        <result property="loginAccount"  column="login_account"/>
        <result property="phone"         column="phone"/>
        <result property="email"         column="email"/>
        <result property="role"          column="role"/>
        <result property="status"        column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime"    column="create_time"/>
        <result property="nickname"      column="nickname"/>
        <result property="avatarUrl"     column="avatar_url"/>
        <result property="gender"        column="gender"/>
    </resultMap>

    <!--
        后台用户列表分页查询（支持 login_account / email / phone 三维度模糊搜索）。
        - is_deleted = 0 过滤逻辑删除数据
        - LEFT JOIN 保证即便 user_detail 不存在也能返回用户基本信息
    -->
    <select id="selectAdminUserPage" resultMap="AdminUserListVoMap">
        SELECT
            u.id,
            u.login_account,
            u.phone,
            u.email,
            u.role,
            u.status,
            u.last_login_time,
            u.create_time,
            ud.nickname,
            ud.avatar_url,
            ud.gender
        FROM `user` u
                 LEFT JOIN user_detail ud ON u.id = ud.user_id
        <where>
            u.is_deleted = 0
            <if test="params.loginAccount != null and params.loginAccount != ''">
                AND u.login_account LIKE CONCAT('%', #{params.loginAccount}, '%')
            </if>
            <if test="params.email != null and params.email != ''">
                AND u.email LIKE CONCAT('%', #{params.email}, '%')
            </if>
            <if test="params.phone != null and params.phone != ''">
                AND u.phone LIKE CONCAT('%', #{params.phone}, '%')
            </if>
        </where>
        ORDER BY u.create_time DESC
    </select>

</mapper>
